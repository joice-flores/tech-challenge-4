import { Screen, EmptyText } from './AdminUsersScreen.styles';

export function AdminUsersScreen() {
  return (
    <Screen>
      <EmptyText>Nenhum usuário encontrado.</EmptyText>
    </Screen>
  );
}
